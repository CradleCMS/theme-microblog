<section class="blog">
    <h1>{{ meta.h1 }}</h1>
    <div class="{{ blog.format }}">{{ blog.html }}</div>
    <hr>
</section>
<section id="main-content" class="article-list">
    {% for article in blog.articles %}
        {% include 'articleCard' %}
    {% endfor %}
</section>