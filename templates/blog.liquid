<section class="blog">
    <h1>{{ meta.h1 }}</h1>
    <div class="{{ blog.format }}">{{ blog.html }}</div>
    <hr>
</section>
<section class="article-list">
<dl>
    <hr>
    {% for article in blog.articles %}
        {% include 'articleCard' %}
        <hr>
	{% endfor %}
</dl>
</section>